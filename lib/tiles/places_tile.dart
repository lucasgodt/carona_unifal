import 'package:flutter/material.dart';

class PlacesTile extends StatelessWidget {

  List<String> places;

  PlacesTile(this.places);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
